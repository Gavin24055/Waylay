# Waylay — Personal AI Life OS
# Python 3.11+ required
# Install: pip install -r requirements.txt

# ── Voice Pipeline ────────────────────────────────────────────────────────────
# webrtcvad — VAD always-on listening (replaces openwakeword)
webrtcvad==2.0.10
pygame==2.6.1
piper-tts==1.4.1            # Offline TTS fallback (optional but recommended)
SpeechRecognition>=3.10.0
edge-tts>=6.1.12            # PRIMARY TTS — Microsoft neural voices, free, no API key
pyaudio>=0.2.14             # Microphone access

# ── LLM ───────────────────────────────────────────────────────────────────────
# Ollama: install separately → https://ollama.com
# Then: ollama serve && ollama pull qwen2.5:7b && ollama pull llama3.2:3b
groq==1.1.1

# ── Memory ────────────────────────────────────────────────────────────────────
chromadb==1.5.5
sentence-transformers==5.3.0

# ── PC Control ───────────────────────────────────────────────────────────────
pyautogui==0.9.54
psutil==7.2.2
pillow==12.1.1
pytesseract==0.3.13
send2trash>=1.8.3            # Safe file deletion (Trash/Recycle Bin)
pygetwindow>=0.0.9           # Active window title on Windows

# ── Google APIs ──────────────────────────────────────────────────────────────
google-api-python-client==2.193.0
google-auth-oauthlib==1.3.0

# ── Microsoft Graph ──────────────────────────────────────────────────────────
# No extra packages needed — uses requests (already listed)

# ── Info Tools ───────────────────────────────────────────────────────────────
duckduckgo-search>=8.1.1     # Web search (pip install -U duckduckgo-search)
requests==2.32.5

# ── Finance / Data ───────────────────────────────────────────────────────────
yfinance==1.2.0

# ── Proactive Systems ────────────────────────────────────────────────────────
apscheduler==3.11.2

# ── Phone / ADB ──────────────────────────────────────────────────────────────
phone-mcp==0.3.6

# ── Projects / Git ───────────────────────────────────────────────────────────
gitpython==3.1.46

# ── Core ──────────────────────────────────────────────────────────────────────
python-dotenv==1.1.0
numpy<2
pytz
requests

# ── Optional: UI Tray ─────────────────────────────────────────────────────────
# macOS/Windows tray icon — uncomment if you want the menu bar icon
# pystray==0.19.5
